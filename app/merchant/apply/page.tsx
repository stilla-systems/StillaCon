import { ApplyForm } from "@/components/merchant-experience"
export default function MerchantApplyPage() { return <ApplyForm /> }
export const metadata = { title: "Apply to become a StillaCon Merchant" }
