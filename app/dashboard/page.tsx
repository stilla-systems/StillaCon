import Link from "next/link"
import { ArrowUpRight, CalendarDays, ChevronRight, CreditCard, Laptop, RefreshCw, ShieldCheck, Smartphone, Wifi } from "lucide-react"
import { Button } from "@/components/ui/button"
import { AppShell, SectionTitle, StatusBadge } from "@/components/stillacon-shell"
import { getCustomerDashboardView } from "@/lib/services"

export default async function Dashboard() { const { customer, devices, payments, plan, subscription } = await getCustomerDashboardView(); return <AppShell><SectionTitle eyebrow="CUSTOMER DASHBOARD" title="Your connection, at a glance." description={`Welcome back, ${customer.name.split(" ")[0]}. Here is the latest on your StillaCon service.`} action={<Button><RefreshCw data-icon="inline-start" />Renew plan</Button>} /><div className="grid gap-4 lg:grid-cols-[1.4fr_1fr_1fr]"><div className="glass rounded-2xl p-6 lg:p-7"><div className="flex items-start justify-between"><div><p className="text-[10px] font-bold tracking-[.18em] text-primary">CURRENT CONNECTION</p><h3 className="mt-3 text-2xl font-semibold">Connected</h3><p className="mt-1 text-sm text-muted-foreground">StillaCon · East Legon Gateway</p></div><div className="grid size-12 place-items-center rounded-2xl bg-emerald-400/10 text-emerald-300"><Wifi /></div></div><div className="mt-8 flex items-end justify-between"><div><p className="text-4xl font-semibold tracking-tight">25<span className="ml-1 text-base font-normal text-muted-foreground">Mbps</span></p><p className="mt-1 text-xs text-muted-foreground">Current speed limit</p></div><span className="text-xs text-emerald-300">Excellent signal</span></div><div className="mt-7 h-2 overflow-hidden rounded-full bg-secondary"><div className="h-full w-[82%] rounded-full bg-gradient-to-r from-primary to-cyan-200" /></div></div><div className="glass rounded-2xl p-6"><div className="flex items-center justify-between"><p className="text-[10px] font-bold tracking-[.18em] text-primary">SUBSCRIPTION</p><StatusBadge status={subscription.status} /></div><h3 className="mt-5 text-2xl font-semibold">{plan.name}</h3><p className="mt-1 text-sm text-muted-foreground">{plan.price} · {plan.speed}</p><div className="mt-7 flex items-center gap-3 border-t border-border pt-4"><CalendarDays className="size-4 text-muted-foreground" /><div><p className="text-xs text-muted-foreground">Renews in</p><p className="text-sm font-semibold">{subscription.daysRemaining} days</p></div></div></div><div className="glass rounded-2xl p-6"><div className="flex items-center justify-between"><p className="text-[10px] font-bold tracking-[.18em] text-primary">PRIMARY DEVICE</p><StatusBadge status="AUTHORIZED" /></div><div className="mt-5 flex items-center gap-3"><div className="grid size-11 place-items-center rounded-xl bg-secondary"><Smartphone className="size-5 text-primary" /></div><div><h3 className="text-sm font-semibold">{devices[0].name}</h3><p className="text-xs text-muted-foreground">Last seen {devices[0].lastSeen}</p></div></div><Link href="/dashboard/devices" className="mt-7 flex items-center justify-between border-t border-border pt-4 text-xs text-muted-foreground hover:text-foreground">Manage devices <ChevronRight className="size-4" /></Link></div></div><div className="mt-8 grid gap-6 lg:grid-cols-[1.25fr_1fr]"><div className="glass rounded-2xl p-6"><div className="flex items-center justify-between"><div><p className="text-[10px] font-bold tracking-[.18em] text-primary">PAYMENT HISTORY</p><h3 className="mt-2 text-lg font-semibold">Recent payments</h3></div><Link href="/dashboard/payments" className="text-xs text-primary hover:underline">View all</Link></div><div className="mt-5 flex flex-col gap-1">{payments.map(payment => <div key={payment.ref} className="flex items-center justify-between rounded-xl px-3 py-3 hover:bg-secondary/60"><div className="flex items-center gap-3"><div className="grid size-9 place-items-center rounded-lg bg-secondary"><CreditCard className="size-4 text-muted-foreground" /></div><div><p className="text-sm font-medium">{payment.plan} plan</p><p className="text-xs text-muted-foreground">{payment.date} · {payment.ref}</p></div></div><div className="text-right"><p className="text-sm font-semibold">{payment.amount}</p><p className="text-[10px] text-emerald-300">{payment.status}</p></div></div>)}</div></div><div className="rounded-2xl border border-primary/15 bg-primary/5 p-6"><ShieldCheck className="size-6 text-primary" /><h3 className="mt-5 text-lg font-semibold">Need a hand?</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">Our support team can help with connection, payment or device questions.</p><Link href="/dashboard/support" className="mt-6 inline-flex items-center gap-2 text-sm font-medium text-primary">Contact support <ArrowUpRight className="size-4" /></Link></div></div></AppShell> }
